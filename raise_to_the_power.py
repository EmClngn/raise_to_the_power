# Write a function called exponent(base, exp) that returns an int value of base raises to the power of exp.

# pseudo code

# user input for base and exponent numbers
input_base = input("What is your desired base number?: ")
input_exp = input(f"Raise {input_base} to the power of what?: ")
print(input_base)
print(input_exp)
# make a function and inside it use for loop to raise the base to the power of the exp
# make an empty list for append
# call the function
